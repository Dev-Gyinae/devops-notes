It basically focues on the non functional requirements of the application.

An IDP is built (Internal Developer Platform)
- Service to be used are then integrated in the IDP for various internal teams eg An IDP of CircleCI, Pulumi, Argo etc

So a layer is built and made provided so that devs etc can connect to API etc from the platform other than the direct source.
